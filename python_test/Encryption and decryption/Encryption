import base64

user_input = input('What do you want to encode: ')

def encrypt(password):
    encrypted = base64.b64encode(password.encode())
    return(encrypted)

encoded = encrypt(user_input)
print(encoded)