# Title: Port scanner

Date: I forgot

This is just a basic port scanner that can be used on only linux OS because it uses unistd.h. This scanner send a probe to each of the port available on the device and listens for a response. It also has a timeout timer in case it took too long.

From this project, I learnt about how port scanner works.
